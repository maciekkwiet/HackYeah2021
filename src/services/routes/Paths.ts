export enum Paths {
  Dashboard = '/',
  Inventory = 'inventory',
  Transactions = 'transactions',
  Transaction = 'transaction/:id',
  FindInventory = 'find-inventory',
  FindNeeds = 'find-needs',
  Needs = 'needs',
  Login = 'login',
  Register = 'register',
}
